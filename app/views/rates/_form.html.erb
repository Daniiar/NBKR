<%= form_with model: @rate, local: true do |form| %>
 
  <% if @rate.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@rate.errors.count, "error") %> prohibited this rate from being saved:
      </h2>
      <ul>
        <% @rate.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :date %><br>
    <%= form.text_field :date %>
  </p>
 
  <p>
    <%= form.label :currency %><br>
    <%= form.text_area :currency %>
  </p>

  <p>
    <%= form.label :exchange_rate %><br>
    <%= form.text_area :exchange_rate %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>